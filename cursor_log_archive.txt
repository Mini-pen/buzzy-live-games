================================================================================
Bloc historique — avant journal Cursor (résumé factuel au 2026-05-14)
================================================================================
Le dépôt buzzy-live-games contenait déjà un MVP fonctionnel :
- Backend Fastify 5 (@fastify/jwt, cors, static), Socket.IO attaché au serveur HTTP.
- Domaine PartyStore (mémoire) : création partie, join, chat, buzz, manches animateur,
  chargement pack quiz depuis games/, JWT joueur et token admin Bearer.
- Nettoyage : sweep périodique (PARTY_MAX_IDLE_MS / PARTY_SWEEP_INTERVAL_MS).
- SPA React Router : accueil, création, rejoindre, /party/:id/play, /party/:id/admin.
- Dockerfile multi-stage ; docker-compose à la racine ; manuel Traefik rédigé.

Lacunes notables avant la session « doc + journal » : pas de fichier cursor_log ;
DEV_PLAN listait encore des tâches déjà faites ; affichage des questions depuis le pack
non exposé comme texte riche dans l’UI joueur (indexes seulement dans le snapshot).

================================================================================
Session 2026-05-14 — Documentation + journal de bord
================================================================================
- Ajout cursor_log_latest / archive ; refonte DEV_PLAN.md sur l’état réel du code.
- README, manuel : liens journal + variables PARTY_SWEEP / PARTY_MAX_IDLE_MS.
- Admin SPA : QRCodeSVG (qrcode.react) pour le lien rejoindre.

================================================================================
Session 2026-05-14 — Déploiement Docker / Traefik (site HS)
================================================================================
- Conflit Traefik : deux stacks (compose project partygames + buzzy-live-games) avec les mêmes
  routeurs buzzylive-* → duplicate router / 404.
- Script scripts/deploy-docker-stack.sh : down, rmi image locale, purge conteneurs partygames,
  build --no-cache, up -d, restart Traefik (~/dev/traefik par défaut).
- Docs TRAEFIK.md, manuel.md, README.md mises à jour pour ce flux.

================================================================================
Session 2026-05-14 — Page Rejoindre : choix son buzzer
================================================================================
- Chargement `GET /api/sounds` ; menu déroulant par groupes (neutre / bon / mauvais) ;
  `buzzSoundKey` envoyé sur `POST /api/parties/:id/join` ; bouton « Écouter » (préécoute).
- Fichiers : `webserver/client/src/App.tsx`, `webserver/client/src/styles/buzzy.css`.
- Commit `f5cf035` sur `main`.
